#!/usr/bin/env bash
#FizzBuzz
i=0
for (( i=0 ; i<101 ; i++ ))
do
	if ! (( i % 15 )) 
	then
		echo "FizzBuzz"
	elif ! (( i % 3 ))
	then
		echo "Fizz"
	elif ! (( i % 5 ))
	then
		echo "Buzz"
	else
		echo "$i"
	fi
done
